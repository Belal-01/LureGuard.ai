# Screenshots and samples

Files here are used in the main README. All Grafana shots are from a local lab stack.

| File | Used for |
|------|----------|
| `opencode-2h-triage.png` | Hero — triage session in opencode |
| `doctor.png` | `make doctor` green output |
| `grafana-soc-overview.png` | SOC Overview dashboard |
| `grafana-assets-containers.png` | Containers & Assets dashboard |
| `grafana-soc-overview-2.png` | Alternate SOC crop (spare) |
| `report-triage-sample.pdf` | Sample incident PDF — triage on RFC5737/test IPs only, no real host CVE dump |

The sample PDF is copied from `reports/20260618-triage---last-2-hours.pdf`. It uses `127.0.0.1` and `198.51.100.77` (documentation range), verdict false_positive, and includes chart embeds. Safe to show publicly.

To refresh: re-capture after UI changes, replace files in place, keep filenames stable so README links don't break.
