# LureGuard Core Configuration
# Generated by install.sh — edit manually or re-run install.sh

thresholds:
  t1: 0.40
  t2: 0.70

llm:
  provider: disabled       # ollama | openai | anthropic | openai_compatible | disabled
  model: ""
  base_url: ""
  timeout_seconds: 60
  max_tokens: 512

cowrie_profiles:
  dev-server:
    host: cowrie-dev
    port: 2222
  db-server:
    host: cowrie-db
    port: 2223

window_seconds: 300
tick_interval_seconds: 2
dnat_ttl_minutes: 60
