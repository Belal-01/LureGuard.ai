# Secrets — never commit
secrets/
*.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.coverage
htmlcov/
dist/
*.egg-info/

# Pre-trained classifier ships in Git: ml/models/model.joblib, scaler.joblib
# (do not gitignore — teammates should not need `make train` on first clone)

# Training data (large — auto-download via kagglehub when running `make train`)
ml/datasets/true_labeled_dataset.csv
ml/datasets/*.csv
ml/datasets/hf-wazuh-alerts.json
ml/datasets/ait-ads/
ml/models/.mplcache/
ml/models/training_*.png

# Docker
.docker/
grafana/Grafana_Data/

# Ali's prototype branch committed runtime DB dumps — never track
DataBase/postgres_data/

# Generated incident reports (keep directory via .gitkeep)
reports/*.md
reports/*.pdf
reports/assets/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/
